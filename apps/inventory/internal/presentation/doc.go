// Package presentation holds the inventory service's HTTP handlers and
// middleware. Handlers stay thin and delegate to the application layer's
// use cases. Populated in a later phase task.
package presentation
