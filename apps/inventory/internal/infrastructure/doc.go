// Package infrastructure holds adapters that implement the application
// layer's ports: Postgres repositories (postgres/), the real QStash event
// publisher and signature verifier (qstash/), and the in-memory fake
// publisher used by tests and local dev (fake/).
package infrastructure
