// Package infrastructure holds adapters that implement the application
// layer's ports: Postgres repositories, the QStash event publisher, and the
// in-memory fake publisher used by tests and local dev. Populated in a
// later phase task.
package infrastructure
