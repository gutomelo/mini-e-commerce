{
  "name": "inventory",
  "version": "0.1.0",
  "private": true,
  "description": "Thin Turborepo wrapper for the Go inventory service",
  "scripts": {
    "build": "go build ./...",
    "dev": "go run ./cmd/server",
    "lint": "test -z \"$(gofmt -l .)\" && go vet ./...",
    "test": "go test ./...",
    "migrate": "go run ./cmd/migrate"
  }
}
